"""Bundled (optional) plugins for the Instructor Contact System.

This package contains Chico State-specific and other “batteries included”
implementations (FacilitiesLink CSV loader, Zoom CSV matcher, Active Directory
matchers, etc.).

In the Option B architecture, the core system can run without this package by
using external `module:attr` plugins instead.
"""
